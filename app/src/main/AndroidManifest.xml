<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:installLocation="internalOnly"
    tools:ignore="Instantiatable">

    <permission
        android:name="com.itsaky.androidide.permission.BIND_LOG_SERVICE"
        android:description="@string/permdesc_bind_logger_service"
        android:label="@string/permlab_bind_logger_service"
        android:protectionLevel="normal"
        tools:ignore="ManifestOrder" />

    <permission android:name="${applicationId}.debug.RECEIVE_WADB_PAIR_RESULT"
        android:protectionLevel="signature"/>

    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />

    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <uses-permission android:name="com.itsaky.androidide.permission.BIND_LOG_SERVICE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Required so voice/speech plugins (e.g. speech-to-text) can record audio. Plugins run
         under this app's UID, so the runtime permission must be declared here — a plugin
         manifest's uses-permission is not merged into the host APK. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission
        android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.REQUEST_DELETE_PACKAGES" />
    <uses-permission android:name="android.permission.ENFORCE_UPDATE_OWNERSHIP" />
    <uses-permission android:name="android.permission.UPDATE_PACKAGES_WITHOUT_USER_ACTION" />

    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <uses-permission android:name="${applicationId}.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION" />
    <uses-permission android:name="${applicationId}.debug.RECEIVE_WADB_PAIR_RESULT" />
    
    <application
        android:name=".app.IDEApplication"
        android:allowBackup="false"
        android:allowNativeHeapPointerTagging="false"
        android:enableOnBackInvokedCallback="true"
        android:extractNativeLibs="true"
        android:hardwareAccelerated="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:preserveLegacyExternalStorage="true"
        android:requestLegacyExternalStorage="true"
        android:resizeableActivity="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidIDE"
        android:usesCleartextTraffic="true"
        tools:ignore="DiscouragedApi,MediaCapabilities,UnusedAttribute"
        tools:replace="android:supportsRtl,android:theme">
        <activity
            android:name=".activities.CrashHandlerActivity"
            android:exported="true"
            android:windowSoftInputMode="stateAlwaysHidden">
            <intent-filter>
                <action android:name="com.itsaky.androidide.REPORT_CRASH" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.SplashActivity"
            android:exported="true"
            android:theme="@style/AppTheme.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.OnboardingActivity"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize"
            android:exported="false" />
        <activity
            android:name=".activities.MainActivity"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize"
            android:exported="true"
            android:theme="@style/Theme.AndroidIDE" />
        <activity
            android:name=".activities.editor.EditorActivityKt"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|fontScale"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".activities.PreferencesActivity"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize" />
        <!-- uiMode/locale/fontScale/density are handled here (not just orientation etc.) because a
             recreation mid-dialog (e.g. a system dark/light auto-switch) would otherwise dismiss
             the forwarded-install dialog while the ViewModel's one-shot "already handled" guard
             (markPendingInstallHandled) silently blocks it from ever being shown again - ADFA-4934. -->
        <activity
            android:name=".activities.PluginManagerActivity"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|uiMode|locale|fontScale|density" />
        <activity
            android:name=".activities.PluginScreenActivity"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize"
            android:exported="false">
            <intent-filter>
                <action android:name="com.itsaky.androidide.plugins.OPEN_PLUGIN_SCREEN" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <!-- Receives a .cgp/.cgt file opened from outside the app (e.g. an email attachment).
             Neither extension has a registered MIME type, so the incoming intent may be typed
             (e.g. "application/octet-stream") or untyped (a bare Uri with no type) depending on
             the sending app. Within a single <intent-filter>, a mimeType on any <data> element
             applies to the WHOLE filter (verified via `dumpsys package`: every <data> tag in a
             filter reports the same "StaticType", even ones with no android:mimeType attribute
             of their own) - so a mimeType-bearing and a mimeType-less variant can't share one
             filter without the mimeType contaminating the mimeType-less one too. `content` gets
             OS leniency and matches even when the incoming intent has no explicit type, so one
             filter with mimeType="*/*" covers both typed and untyped content:// intents; `file`
             gets no such leniency and needs its typed and untyped cases in separate filters.
             Both extensions are combined into each filter below (safe: every <data> tag within
             a given filter shares the same mimeType-or-not shape, so there's no cross-extension
             contamination), and each extension gets both a lowercase and an UPPERCASE pathPattern
             - android:pathPattern has no case-insensitive mode, and some senders (e.g. archives
             re-exported from Windows tools) produce uppercase extensions.
             android:host="*" is required alongside pathPattern - the manifest matcher only
             evaluates pathPattern when host is also present.
             Known limitation, not fixable via manifest matching: a sender whose content:// Uri
             path never carries the filename/extension at all (some email providers' attachment
             Uris look like content://.../message_attachment/12345/0/ATTACHMENT/false) can't match
             a pathPattern-based filter regardless of type. The only alternative - a pathPattern-
             less, mimeType="*/*" filter - would register this app as a candidate handler for
             every file view intent on the device, which is a worse tradeoff than missing those
             senders. -->
        <!-- uiMode/locale/fontScale/density are handled here (not just orientation etc.) because a
             recreation mid-dialog (e.g. a system dark/light auto-switch) would otherwise reset the
             Compose dialog state to nothing while the ViewModel's one-shot receivedUriGate guard
             silently blocks the effect from ever being resent - ADFA-4934. -->
        <activity
            android:name=".activities.ExternalFileInstallActivity"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|uiMode|locale|fontScale|density"
            android:excludeFromRecents="true"
            android:exported="true"
            android:theme="@style/Theme.AndroidIDE">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" android:host="*" android:mimeType="*/*" android:pathPattern=".*\\.cgp" />
                <data android:scheme="content" android:host="*" android:mimeType="*/*" android:pathPattern=".*\\.CGP" />
                <data android:scheme="content" android:host="*" android:mimeType="*/*" android:pathPattern=".*\\.cgt" />
                <data android:scheme="content" android:host="*" android:mimeType="*/*" android:pathPattern=".*\\.CGT" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="file" android:host="*" android:mimeType="*/*" android:pathPattern=".*\\.cgp" />
                <data android:scheme="file" android:host="*" android:mimeType="*/*" android:pathPattern=".*\\.CGP" />
                <data android:scheme="file" android:host="*" android:mimeType="*/*" android:pathPattern=".*\\.cgt" />
                <data android:scheme="file" android:host="*" android:mimeType="*/*" android:pathPattern=".*\\.CGT" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="file" android:host="*" android:pathPattern=".*\\.cgp" />
                <data android:scheme="file" android:host="*" android:pathPattern=".*\\.CGP" />
                <data android:scheme="file" android:host="*" android:pathPattern=".*\\.cgt" />
                <data android:scheme="file" android:host="*" android:pathPattern=".*\\.CGT" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.AboutActivity" />
        <activity
            android:name=".activities.editor.FAQActivity"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize" />
        <activity
            android:name=".activities.editor.HelpActivity"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize" />
        <activity
            android:name=".activities.TerminalActivity"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize"
            android:windowSoftInputMode="adjustResize" />

        <!-- Crash/log reporting. These io.sentry.* keys configure the Sentry SDK,
             which reports to our GlitchTip backend (GlitchTip speaks the Sentry
             protocol); ${sentryDsn} carries the GlitchTip DSN. -->
        <!-- Required: the GlitchTip project identifier (DSN) -->
        <meta-data
            android:name="io.sentry.dsn"
            android:value="${sentryDsn}" />
        <!-- enable automatic breadcrumbs for user interactions (clicks, swipes, scrolls) -->
        <meta-data
            android:name="io.sentry.traces.user-interaction.enable"
            android:value="true" />
        <!-- enable screenshot for crashes -->
        <meta-data
            android:name="io.sentry.attach-screenshot"
            android:value="true" />
        <!-- enable view hierarchy for crashes -->
        <meta-data
            android:name="io.sentry.attach-view-hierarchy"
            android:value="true" />
        <!-- enable the performance API by setting a sample-rate, adjust in production env -->
        <meta-data
            android:name="io.sentry.traces.sample-rate"
            android:value="0.0" />
        <meta-data
            android:name="io.sentry.send-default-pii"
            android:value="true" />
        <!-- Enable logs to be sent to GlitchTip -->
        <meta-data
            android:name="io.sentry.logs.enabled"
            android:value="true" />

        <!-- Suppress SDK auto-init; re-enabled at runtime only with telemetry consent -->
        <meta-data
            android:name="io.sentry.auto-init"
            android:value="false" />
        <meta-data
            android:name="firebase_analytics_collection_enabled"
            android:value="false" />
        <meta-data
            android:name="firebase_data_collection_default_enabled"
            android:value="false" />
        <provider
            android:name=".provider.IDEDocumentsProvider"
            android:authorities="com.itsaky.androidide.documents"
            android:exported="true"
            android:grantUriPermissions="true"
            android:permission="android.permission.MANAGE_DOCUMENTS">
            <intent-filter>
                <action android:name="android.content.action.DOCUMENTS_PROVIDER" />
            </intent-filter>
        </provider>

        <provider
            android:name=".provider.IDEFileProvider"
            android:authorities="${applicationId}.providers.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/ide_file_provider_paths" />
        </provider>

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">

            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <receiver
            android:name=".services.InstallationResultReceiver"
            android:enabled="true"
            android:exported="true"
            tools:ignore="ExportedReceiver">
            <intent-filter>
                <action android:name="com.itsaky.androidide.installer.INSTALL_PACKAGE" />
            </intent-filter>
        </receiver>

        <!--
         Receiver used to receive broadcasts when foreground application changes
         This receiver *must* have permissions that are not usually granted to
         normal Android applications so that only the `shell` user can send
         broadcasts to this receiver.
         -->
        <receiver
            android:name=".services.debug.ForegroundAppReceiver"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL"
            android:multiprocess="false"
            tools:ignore="ExportedReceiver">
            <intent-filter>
                <!-- Must be the same as the one defined in ShizukuServerConstants.java -->
                <action android:name="com.itsaky.androidide.shizuku.ACTION_FOREGROUND_CHANGED" />
            </intent-filter>
        </receiver>

        <service
            android:name=".services.builder.GradleBuildService"
            android:exported="false" />

        <service
            android:name=".services.debug.DebuggerService"
            android:exported="false" />

        <service
            android:name=".services.log.LogReceiverService"
            android:exported="true"
            android:permission="com.itsaky.androidide.permission.BIND_LOG_SERVICE">
            <intent-filter>
                <action android:name="com.itsaky.androidide.LOG_SERVICE_ACTION" />
            </intent-filter>
        </service>
        <service
            android:name="androidx.appcompat.app.AppLocalesMetadataHolderService"
            android:enabled="false"
            android:exported="false">
            <meta-data
                android:name="autoStoreLocales"
                android:value="true" />
        </service>
    </application>
</manifest>
