package org.secuso.privacyfriendlysudoku.ui

import android.app.Activity

// Scaffolding, not vendored: the real MainActivity is the whole game/board UI. The vendored
// GeneratorService.java only constructs an Intent(context, MainActivity::class.java) for its
// notification's PendingIntent, never calling into the real Activity's body.
class MainActivity : Activity()
