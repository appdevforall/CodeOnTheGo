package org.secuso.privacyfriendlytodolist.view

import android.app.Activity

// Scaffolding, not vendored: the real MainActivity is the whole task-list UI (RecyclerView,
// calendar, PIN lock, tutorial). The vendored subgraph's receivers/jobs never call into it --
// it exists only as the manifest's launcher entry point.
class MainActivity : Activity()
