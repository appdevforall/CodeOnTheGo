package io.github.rosemoe.sora.corpusharness

/**
 * Harness scaffolding (ADFA-4128 D4) -- the "sample-app UI edit" scenario edits
 * [INITIAL] the way a real host app's UI code would edit a displayed constant.
 * Not part of upstream sora-editor.
 */
object SampleText {
	const val INITIAL = "// quick-build corpus harness\nfun greet() {}\n"
}
