package org.secuso.privacyfriendlynotes.ui

import android.app.Activity

// Scaffolding, not vendored: the real SplashActivity does startup migration/theming work.
// This subgraph doesn't launch it; it exists only as the manifest's launcher entry point.
class SplashActivity : Activity()
