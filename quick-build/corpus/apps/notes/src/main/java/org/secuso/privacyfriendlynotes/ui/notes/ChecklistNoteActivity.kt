package org.secuso.privacyfriendlynotes.ui.notes

// Scaffolding, not vendored: same rationale as TextNoteActivity -- a notification-dispatch target only.
class ChecklistNoteActivity : BaseNoteActivity()
