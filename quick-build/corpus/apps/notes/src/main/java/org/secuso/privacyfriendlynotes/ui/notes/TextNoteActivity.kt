package org.secuso.privacyfriendlynotes.ui.notes

// Scaffolding, not vendored: the vendored NotificationReceiver.kt only constructs an
// Intent(context, TextNoteActivity::class.java), never calling into the real editor's body.
class TextNoteActivity : BaseNoteActivity()
