package com.secuso.privacyfriendlycodescanner.qrscanner.ui.activities

import android.app.Activity

// Scaffolding, not vendored: the real ScannerActivity drives the camera scan UI. Nothing in
// this subgraph (the Room history DB + ZXing barcode-bitmap generator) calls into it; it
// exists only as the manifest's launcher entry point.
class ScannerActivity : Activity()
