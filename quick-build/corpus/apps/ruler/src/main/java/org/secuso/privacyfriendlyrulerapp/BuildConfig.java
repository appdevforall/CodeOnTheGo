package org.secuso.privacyfriendlyrulerapp;

/**
 * Harness scaffolding: AGP normally generates this class from the build.gradle
 * defaultConfig (see vendor.json); the corpus harness only runs aapt2 + javac/kotlinc,
 * not AGP, so AboutActivity's BuildConfig.VERSION_NAME reference needs this stand-in.
 * Not vendored -- checked in like the rest of the app's scaffolding (see README.md).
 */
public final class BuildConfig {
	public static final String VERSION_NAME = "1.1.4";

	private BuildConfig() {}
}
