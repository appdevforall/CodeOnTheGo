package org.appdevforall.cotg.corpus.medium.formatters

/** Formats numeric values for display. Leaf class: no other file depends on its internals. */
class NumberFormatter {
	fun format(value: Int): String = "QB_MARKER_STEP1:$value"
}
