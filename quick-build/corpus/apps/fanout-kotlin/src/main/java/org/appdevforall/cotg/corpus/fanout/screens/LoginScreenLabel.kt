package org.appdevforall.cotg.corpus.fanout.screens

import org.appdevforall.cotg.corpus.fanout.core.Constants

/** Section label for the Login screen. References the app-wide const val label. */
class LoginScreenLabel {
	fun title(): String = "${Constants.APP_LABEL} — Login"
}
