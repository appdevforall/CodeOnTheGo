package org.appdevforall.cotg.corpus.fanout.screens

import org.appdevforall.cotg.corpus.fanout.core.Constants

/** Section label for the Referral screen. References the app-wide const val label. */
class ReferralScreenLabel {
	fun title(): String = "${Constants.APP_LABEL} — Referral"
}
