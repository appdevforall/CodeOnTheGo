package org.appdevforall.cotg.corpus.fanout.screens

import org.appdevforall.cotg.corpus.fanout.core.Constants

/** Section label for the Logout screen. References the app-wide const val label. */
class LogoutScreenLabel {
	fun title(): String = "${Constants.APP_LABEL} — Logout"
}
