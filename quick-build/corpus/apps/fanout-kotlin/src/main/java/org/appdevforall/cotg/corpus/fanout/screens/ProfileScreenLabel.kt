package org.appdevforall.cotg.corpus.fanout.screens

import org.appdevforall.cotg.corpus.fanout.core.Constants

/** Section label for the Profile screen. References the app-wide const val label. */
class ProfileScreenLabel {
	fun title(): String = "${Constants.APP_LABEL} — Profile"
}
