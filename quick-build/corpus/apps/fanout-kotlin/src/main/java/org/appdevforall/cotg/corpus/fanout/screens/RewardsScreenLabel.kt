package org.appdevforall.cotg.corpus.fanout.screens

import org.appdevforall.cotg.corpus.fanout.core.Constants

/** Section label for the Rewards screen. References the app-wide const val label. */
class RewardsScreenLabel {
	fun title(): String = "${Constants.APP_LABEL} — Rewards"
}
