package org.appdevforall.cotg.corpus.fanout.screens

import org.appdevforall.cotg.corpus.fanout.core.Constants

/** Section label for the Search screen. References the app-wide const val label. */
class SearchScreenLabel {
	fun title(): String = "${Constants.APP_LABEL} — Search"
}
