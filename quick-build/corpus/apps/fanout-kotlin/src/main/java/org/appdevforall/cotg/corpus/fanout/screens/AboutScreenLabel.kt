package org.appdevforall.cotg.corpus.fanout.screens

import org.appdevforall.cotg.corpus.fanout.core.Constants

/** Section label for the About screen. References the app-wide const val label. */
class AboutScreenLabel {
	fun title(): String = "${Constants.APP_LABEL} — About"
}
