package org.appdevforall.cotg.corpus.fanout.screens

import org.appdevforall.cotg.corpus.fanout.core.Constants

/** Section label for the Home screen. References the app-wide const val label. */
class HomeScreenLabel {
	fun title(): String = "${Constants.APP_LABEL} — Home"
}
