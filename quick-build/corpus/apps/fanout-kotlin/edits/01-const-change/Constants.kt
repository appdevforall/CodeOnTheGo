package org.appdevforall.cotg.corpus.fanout.core

/** `const val` — every referencing file inlines this literal at compile time (no runtime lookup). */
object Constants {
	const val APP_LABEL = "Fanout Demo Rev2"
}
