<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose themes in-code via MaterialTheme{}; the Activity/window only needs
         a plain framework theme. No Material view-library dependency here. -->
    <style name="Theme.Payload" parent="@android:style/Theme.DeviceDefault.Light.NoActionBar" />
</resources>
